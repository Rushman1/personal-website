'use strict';

(function(baseCode) {
	baseCode(window.jQuery, window, document);
}(function($, window, document) {
	$(function() {
		
		// Button Clicks

	});
	// Wrappers

	// AJAX Functions

	// Functions

	// Dynamic Buttons
}));