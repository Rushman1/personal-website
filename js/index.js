(function(baseCode) {
	baseCode(window.jQuery, window, document);
}(function($, window, document) {
	$(function() {

		setFooter();
		hideWaitSymbol();
		
		// Button Clicks

	});
	// Wrappers

	// AJAX Functions

	// Functions

	// Dynamic Buttons
}));